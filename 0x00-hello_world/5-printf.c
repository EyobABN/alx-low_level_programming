#include <stdio.h>
/*
 * This program prints "with proper grammar, but the
 * outcome is a piece of art,." using the function puts
*/

/**
 * main - print a string using the puts function
 * Return: return 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
